apiVersion: v1
kind: Service
metadata:
  name: {{ include "slovolov.fullname" . }}
  labels:
    {{- include "slovolov.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "slovolov.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
